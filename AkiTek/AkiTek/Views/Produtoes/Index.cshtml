@model IEnumerable<AkiTek.Models.Produto>

@{
    ViewBag.Title = "Index";
}

<h2>Catálogo</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Preco)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }

    </table>*@

<div id="categories" class="container">
    <div id="rowCateg" class="row"></div>

    @foreach (var item in Model)
    {
        <div class="col-sm-12 col-lg-3 d-flex align-items-stretch">
            <div class="card m-3" style="width: 18rem;">
                <a href="/Produtoes/Details/@item.ID">
                    <img class="card-img-top" src="@item.ImgUrl" alt="Card header image" height="200" style="object-fit:cover">
                </a>
                    <div class="card-body">
                        <h5 class="card-title">@item.Nome</h5>
                        @*<p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>*@
            </div>
            </div>
        </div>
    }
</div>